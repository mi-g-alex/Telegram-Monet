<resources>
    <style name="MonetTG.DayNight" parent="Theme.Material3.DynamicColors.DayNight">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="MonetTG.Card" parent="Widget.Material3.CardView.Filled">
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:layout_marginTop">10dp</item>
    </style>

    <!-- Base application theme. -->
    <style name="MonetTG" parent="MonetTG.DayNight">
        <!-- StatusBar -->
        <item name="android:windowLightStatusBar">true</item>
        <!-- NavBar -->
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <color name="card_bg">@android:color/system_neutral2_50</color>

    <style name="MonetTG.Title" parent="@style/Widget.Material3.CollapsingToolbar.Large">
        <item name="fontFamily">@font/google_sans_medium</item>
    </style>

    <style name="MonetTG.Button" parent="@style/Widget.Material3.Button">
        <item name="fontFamily">@font/google_sans_medium</item>
    </style>

</resources>